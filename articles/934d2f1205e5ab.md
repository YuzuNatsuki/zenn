---
title: "MacBook Pro M5 Max（RAM 128GB）でローカルLLMを動かす｜購入レビューと実測まとめ"
emoji: "🧠"
type: "idea" # tech: 技術記事 / idea: アイデア
topics: ["mac", "llm", "ollama", "ローカルllm", "macbookpro"]
published: false
---

:::message
本記事は筆者が実際に購入した MacBook Pro（M5 Max / ユニファイドメモリ 128GB）で、ローカルLLMをどこまで快適に動かせるかを検証した購入レビューです。価格・ベンチマーク等の数値は購入時点・検証時点のものです。
:::

## はじめに

「ローカルでLLMを動かすために、メモリは128GB必要なのか？」——これが今回いちばん検証したかったことです。

![](/images/934d2f1205e5ab/20260612161445.png)

クラウドのAPIは手軽ですが、

- 機密データを外に出したくない
- 従量課金を気にせず大量に試行錯誤したい
- オフラインでも動かしたい

といった理由から、手元で大きめのモデルを動かせる環境がほしくなりました。そこで思い切って **MacBook Pro M5 Max・ユニファイドメモリ 128GB** を購入したので、その実体験をまとめます。

:::message
**この記事の要点（TL;DR）**

- 128GBのユニファイドメモリがあると、`（TODO: 実際に常用したモデル名）` クラスまで余裕を持って動かせた
- 速度は `（TODO: tok/s の実測）`。体感は `（TODO: 快適／実用十分／用途次第 などの所感）`
- 「とりあえずローカルLLM」なら64GBでも足りるが、`（TODO: 128GBが効いた具体的な場面）` では128GBが効いた
:::

## 購入した構成

| 項目 | スペック |
|------|----------|
| モデル | MacBook Pro `（TODO: 14 or 16インチ）` |
| チップ | Apple M5 Max（`（TODO: CPU/GPUコア数）`） |
| ユニファイドメモリ | 128GB |
| ストレージ | `（TODO: 容量）` |
| 購入価格 | `（TODO: 価格・購入先）` |

### なぜ128GB / M5 Maxにしたか

- **メモリ:** ローカルLLMはモデルサイズがほぼそのままメモリ要件になる。`（TODO: 何B量子化のモデルを動かしたかったか）` を見据えて128GBにした。
- **チップ:** `（TODO: GPU/メモリ帯域を重視した理由など）`
- **ストレージ:** モデルファイルは1つで数十GBになるので `（TODO: ストレージ選定の理由）`

:::message
ユニファイドメモリはCPUとGPUで共有されるため、VRAMを別に積む必要がない。これがMacでローカルLLMを動かすときの最大の強み。
:::

## セットアップ

### Ollama

最も手軽だったのが [Ollama](https://ollama.com/) です。

```bash
# インストール（Homebrew）
brew install ollama

# モデルの取得と実行
ollama run （TODO: モデル名、例 llama3.1:70b）
```

`（TODO: 導入でつまずいた点があれば）`

### LM Studio

GUIで試したい場合は [LM Studio](https://lmstudio.ai/) も使いました。`（TODO: 使用感）`

## 128GBで実際に何が動くか

モデルは「パラメータ数 × 量子化ビット数」でおおよそのメモリ消費が決まります。目安は以下のとおり（量子化や実装で変動します）。

| モデル規模 | 量子化 | 必要メモリの目安 | 128GBでの所感 |
|------|------|------|------|
| 〜8B | 4bit | 〜6GB | `（TODO）` 余裕 |
| 〜32B | 4bit | 〜20GB前後 | `（TODO）` |
| 〜70B | 4bit | 〜40GB前後 | `（TODO）` |
| 100B超 / MoE | 4bit | `（TODO）` | `（TODO）` |

:::message
表の「必要メモリの目安」はあくまで概算です。実際にはコンテキスト長やKVキャッシュでさらにメモリを消費します。
:::

## 実測

検証したモデルと結果です。

| モデル | 量子化 | メモリ使用量 | 生成速度 | 所感 |
|------|------|------|------|------|
| `（TODO）` | `（TODO）` | `（TODO）GB` | `（TODO）tok/s` | `（TODO）` |
| `（TODO）` | `（TODO）` | `（TODO）GB` | `（TODO）tok/s` | `（TODO）` |

> 計測条件：`（TODO: プロンプト長 / コンテキスト長 / 計測方法）`

### 発熱・ファン・バッテリー

- 発熱: `（TODO）`
- ファン: `（TODO）`
- バッテリー駆動時: `（TODO）`

## 使ってみて感じたこと

### よかった点

- `（TODO）`
- `（TODO）`

### いまいちだった点 / 注意点

- `（TODO）`
- `（TODO）`

## 128GBは必要だったのか？

`（TODO: 結論。どんな人なら64GBで十分か、どんな用途なら128GBを推すか）`

## まとめ

- `（TODO: 一番伝えたい結論）`
- ローカルLLM目的でMacを選ぶなら、`（TODO: メモリ容量の指針）`
- `（TODO: これから買う人へのアドバイス）`

---

## 参考リンク

- [Ollama](https://ollama.com/)
- [LM Studio](https://lmstudio.ai/)
- `（TODO: 参考にした記事・公式ドキュメントなど）`
